import React from 'react'

function searchResults({search}) {
    return (
      <div>
        {/* {search?.map((oneSearch) => (
          <>
            <h1>{oneSearch.title}</h1>
          </>
        ))} */}
      </div>
    );
}

export default searchResults
